# Build WAMP (npm run build:wamp) : TOUTES les données vont dans MySQL
# via wamp_deploy/api/*. Ne jamais mettre ce flag dans .env (dev) :
# le développement local doit rester en mode navigateur (IndexedDB).
VITE_WAMP_MODE=1
